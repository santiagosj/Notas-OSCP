###📌 FTP – Comandos básicos

```bash
# Conexión
ftp <host>                # Conectarse al servidor FTP
ftp -p <host>              # Conexión directa en modo pasivo
ftp <host> <puerto>        # Especificar puerto (por defecto 21)

# Login
Name: <usuario>            # Usuario (ej: anonymous)
Password: <contraseña>     # Contraseña (puede ser email si es anonymous)

# Navegación
pwd                        # Mostrar directorio actual remoto
ls                         # Listar archivos/carpeta en servidor
dir                        # Listado detallado
cd <ruta>                  # Cambiar directorio en servidor
lcd <ruta>                 # Cambiar directorio local

# Transferencia simple
get <archivo>              # Descargar archivo
put <archivo>              # Subir archivo

```
### 📌 FTP – Nivel intermedio

```bash
# Modos de transferencia
ascii                      # Modo texto (archivos de texto)
binary                     # Modo binario (archivos binarios, .exe, .zip, .jpg)

# Múltiples transferencias
mget <patrón>              # Descargar varios archivos (ej: mget *.txt)
mput <patrón>              # Subir varios archivos

# Eliminar y renombrar
delete <archivo>           # Eliminar archivo remoto
mdelete <patrón>           # Eliminar varios archivos
rename <antiguo> <nuevo>   # Renombrar archivo

# Crear/eliminar directorios
mkdir <directorio>         # Crear directorio remoto
rmdir <directorio>         # Eliminar directorio remoto

```
### 📌 FTP – Opciones de conexión y modos

```bash
# Ver/activar modo pasivo (cliente clásico)
passive                    # Alternar modo pasivo ON/OFF

# Mostrar estado de conexión
status                     # Información actual (modo, tipo, conexión)

# Cambiar tipo de conexión (raro en la práctica)
mode stream                # Modo por defecto
mode block                 # Modo bloque (poco usado)
mode compressed            # Modo comprimido (poco usado)
```

### 📌 FTP – Comandos avanzados

```bash
# Transferencia recursiva (depende del cliente)
mirror                     # (En lftp) Descargar/subir directorio completo
mirror -R                  # (En lftp) Subir directorio completo

# Ejecutar comandos locales sin salir de FTP
!ls                        # Listar en tu máquina local
!pwd                       # Ver carpeta local actual
!mkdir test                # Crear carpeta local

# Cambiar permisos (si el servidor lo permite)
chmod <modo> <archivo>     # Ej: chmod 755 index.html

# Cambiar propietario (si permitido)
chown <usuario> <archivo>  # Ej: chown user1 file.txt
```
### 📌 Ejemplo de sesión segura y limpia

```bash
ftp -p 192.168.205.145
# Usuario y pass
binary
passive
pwd
ls
get archivo.zip
quit
```
### 📌 lftp – Comandos básicos
```bash
# Conexión
lftp <host>                         # Conectar al servidor (pedirá usuario/pass)
lftp -u <usuario>,<contraseña> <host>   # Conectar con credenciales
lftp -u anonymous, <host>           # Conexión anónima

# Ejemplo
lftp -u anonymous, 192.168.205.145

# Navegación
pwd                                 # Mostrar ruta actual en servidor
ls                                  # Listar contenido remoto
cd <ruta>                           # Cambiar carpeta remota
lcd <ruta>                          # Cambiar carpeta local

# Transferencia simple
get <archivo>                       # Descargar archivo
put <archivo>                       # Subir archivo
```

### 📌 lftp – Nivel intermedio

```bash
# Modos de transferencia
set ftp:passive-mode on             # Activar modo pasivo
set ftp:passive-mode off            # Desactivar modo pasivo
set ftp:ssl-allow no                 # Desactivar SSL/TLS si da problemas

# Transferencia múltiple
mget <patrón>                        # Descargar varios archivos
mput <patrón>                        # Subir varios archivos

# Eliminar y renombrar
rm <archivo>                         # Eliminar archivo remoto
mv <antiguo> <nuevo>                 # Renombrar archivo remoto

# Directorios
mkdir <directorio>                   # Crear carpeta remota
rmdir <directorio>                   # Eliminar carpeta remota
```

### 📌 lftp – Avanzado y scripting

```bash
# Transferencia recursiva
mirror                               # Descargar directorio completo
mirror -R                            # Subir directorio completo
mirror --continue                    # Reanudar transferencia interrumpida
mirror --delete                      # Eliminar archivos locales que no estén en el servidor
mirror -R --delete                   # Sincronizar (local → remoto)

# Control de velocidad
set net:limit-rate 500K:1M            # Límite subida: 500KB/s, descarga: 1MB/s

# Reintentos automáticos
set net:max-retries 5                 # Número máximo de reintentos
set net:reconnect-interval-base 5     # Intervalo inicial (segundos)
set net:reconnect-interval-max 30     # Intervalo máximo (segundos)

# Ejecutar comandos locales
!ls                                   # Listar en local
!pwd                                  # Ruta local

# Salir
exit
bye
```
### 📌 Ejemplo de sesión completa en modo pasivo

```bash
lftp -u anonymous, 192.168.205.145
set ftp:passive-mode on
set ftp:ssl-allow no
binary
ls
mirror /remote/path /local/path
bye
```
